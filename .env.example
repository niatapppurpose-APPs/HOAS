# VITE Environment Variables
# Copy this file to .env and fill in your Firebase credentials

# Firebase Configuration
VITE_FIREBASE_API_KEY=AIzaSyAI_ShJZHclMdhq_ESF1PHneb8D6RR-VH8
VITE_FIREBASE_AUTH_DOMAIN=hoas-65dee.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=hoas-65dee
VITE_FIREBASE_STORAGE_BUCKET=hoas-65dee.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=934000888542
VITE_FIREBASE_APP_ID=1:934000888542:web:2a4ef0198ecf171bded04f

# Firebase Emulator Configuration
# Set to 'true' to enable automatic emulator detection in development
# The app will automatically detect if emulators are running and:
#   - Use emulators if available (localhost:9099, localhost:8080, etc.)
#   - Gracefully fall back to production Firebase if not running
# Set to 'false' to always use production Firebase
# Note: Only works in development mode (npm run dev), ignored in production builds
VITE_USE_FIREBASE_EMULATOR=true

# Emulator Ports (must match firebase.json)
# Auth Emulator: http://localhost:9099
# Firestore Emulator: http://localhost:8080
# Functions Emulator: http://localhost:5001
# Emulator UI: http://localhost:4000
