import Tooltip from './ui/Tooltip';

/**
 * Tooltip wrapper component for backwards compatibility.
 * Delegates to the new Vengence UI / Untitled UI animated Tooltip component.
 */
export default Tooltip;
