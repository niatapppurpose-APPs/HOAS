import Breadcrumbs from './ui/Breadcrumbs';

/**
 * Breadcrumbs wrapper component for backwards compatibility.
 * Delegates to the new Untitled UI styled Breadcrumbs component.
 */
export default Breadcrumbs;
